# HackUPC

To validate programmatic Cursor agent invocation (CLI and optional TypeScript SDK), see [docs/cursor_agent_cli_validation.md](docs/cursor_agent_cli_validation.md).

To try **native handoff** (clipboard + optional `executeCommand` to focus Composer) inside Cursor, see [extension/README.md](extension/README.md) and press **F5** with the repo root opened.