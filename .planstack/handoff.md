# Plan: Add a red page to the extension
## Phase (run only this): Scaffold the red page surface

Execute only the work described in this phase description and its tasks; do not expand scope to other phases unless blocked.

Add a view, command, and webview (or simple UI host) for the new red page, following existing extension patterns.

### Tasks
- **t-register-view** [pending] (commit): Register contribution (package.json) and a command to open the red page.
- **t-webview-html** [pending] (commit): Implement HTML/CSS for a full red-themed page in media or inline resolver.

## Version control context
- Current checkout: feat/hardening-and-improvements

Implement the work in this repository with normal file edits (not JSON-only or plan-file output).

---
*Planstack handoff metadata — safe to delete before sending to Junie.*
planId: add-red-page-extension · phaseId: scaffold · title: Add a red page to the extension › Scaffold the red page surface
